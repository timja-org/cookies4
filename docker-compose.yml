version: '2.1'

services:
  efgh:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: hmctspublic.azurecr.io/abcd/efgh
    ports:
      - $SERVER_PORT:$SERVER_PORT
